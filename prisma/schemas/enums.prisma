enum ShippingSpeed {
    express
    standard
    signature
}

enum SpendingLimitInterval {
    per_authorization
    daily
    weekly
    monthly
    yearly
    all_time
}

enum CardStatus {
    active
    inactive
    canceled
}

enum CardType {
    physical
    virtual
}